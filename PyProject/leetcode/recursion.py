def recursion(level, **params):
    # recursion terminator
    if level > 0:
        return
    # process logic in current level

    # drill down

    # reverse the current level status if needed

    """
    recursion terminator
    process logic in current lvel
    drill down
    reverse the current level status if needed
    
    
    recursion terminator
    
    process logic in the current level
    drill down
    reverse the current status if needed
    
    """
